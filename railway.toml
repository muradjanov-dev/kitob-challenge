[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate --noinput && gunicorn src.wsgi --bind 0.0.0.0:${PORT:-8000} --workers 1 --timeout 120 --log-level debug --capture-output"
restartPolicyType = "on_failure"
